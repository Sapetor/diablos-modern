# Development dependencies for DiaBloS
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=7.0.0
pytest-qt>=4.2.0
pytest-cov>=4.0.0
pytest-timeout>=2.1.0  # CI backstop: fail a hung test instead of blocking the job

# Code quality
ruff>=0.6.0  # linter + formatter (replaces flake8/pylint/isort/black); see pyproject.toml
mypy>=1.0.0

# Type stubs
PyQt5-stubs>=5.15.0
