# Documentation build dependencies.
#
# Used by .github/workflows/docs.yml and by Read the Docs (readthedocs.yaml).
# The runtime packages are here because mkdocstrings imports lib/, blocks/ and
# modern_ui/ to generate the API reference pages.

mkdocs>=1.6
mkdocs-material>=9.5
mkdocstrings[python]>=0.25

numpy
scipy
matplotlib>=3.5.0
tqdm
pyqtgraph>=0.13.0
# Mirrors requirements.txt: PyQt6 6.11 needs Python >= 3.10. The docs build
# runs on 3.12, but keep the marker so a 3.9 build still resolves.
PyQt6>=6.7,<6.11; python_version < "3.10"
PyQt6>=6.7; python_version >= "3.10"
Pillow
